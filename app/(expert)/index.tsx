import { RoleScreen } from "@/components/role-screen";

export default function DoctorDashboard() {
  return (
    <RoleScreen
      description="Review today’s schedule, patient requests, and care activity."
      icon="pulse"
      title="Doctor Dashboard"
    />
  );
}
